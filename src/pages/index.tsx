import { Button } from "@mantine/core";
import { type NextPage } from "next";

const Home: NextPage = () => {

  return (
    <div>
      <Button>Hello</Button>
    </div>
  );
};

export default Home;
